version: '3.9'
services:
  backend:
    image: node:20
    working_dir: /app
    volumes:
      - ./:/app
    command: bash -lc "npm install && npm run dev:backend"
    ports:
      - '3000:3000'
    env_file:
      - apps/backend/.env
