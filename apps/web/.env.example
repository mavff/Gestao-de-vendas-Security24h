# Frontend API base URL (backend Nest)
NEXT_PUBLIC_API_BASE_URL=http://localhost:3001

# Data source mode: api | mock
# Default behavior in code is "api" if this variable is missing.
NEXT_PUBLIC_DATA_SOURCE=api

